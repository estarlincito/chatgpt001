import React from "react";

const Chatgpt001 = ({ children }: { children: React.ReactNode }) => {
  return <div className="w-96 h-[80vh] p-3 duration-500">{children}</div>;
};

export default Chatgpt001;
